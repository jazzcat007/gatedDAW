# Error Triage Knowledge Base

Living index of production error signatures (from `logs.opendaw.studio`). The `/triage-errors` task reads and
updates this file: it matches each unfixed group from `unfixed.php` against the signatures below, annotates
known ones, and starts a fix when the root cause is known.

Status values: `fixed` · `root-cause-known` (fix designed/started, not yet fully shipped/marked) ·
`investigating` · `parked` (mechanism understood but blocked on a repro / the reporter's project) ·
`environmental` · `unknown`.

Match by the error message (a stable substring); the log ids are just examples of when it was seen.

| Status | Error message (signature) | Root cause | Fix / commit | Notes |
|---|---|---|---|---|
| fixed | `The radius provided (-0.125) is negative` | `GonioCard` derived `radius = min(w, h) * 0.5 - 1.0` from the canvas size in device pixels without clamping. `CanvasRenderer` only refuses to paint at `clientWidth/clientHeight === 0`, so a card body squeezed to 1 CSS px (analysis grid is `flex: 1 / min-height: 0 / grid-auto-rows: 1fr`, only the 21px card head is fixed, so dragging the panel splitter nearly closed collapses the body) still painted. `-0.125` = 1px at dpr 1.75 (Chrome at 175% scaling); at dpr 1 it is `-0.5`, so this is not dpr-specific. Arrived as a synchronous `ErrorEvent`, so it took the fatal path | Uncommitted: `insetRadius(w, h, inset)` in `AnalysisCommon.ts` clamps at 0, used by `GonioCard`; `NoteClipPainter`'s `maxRadius` (same latent inset error) clamped to `minRadius`. Test `AnalysisCommon.test.ts` | live id 1107; flip in the DB once deployed |
| root-cause-known | `[DOMException] Failed to start the audio device` | WebKit refused to start the output unit on `AudioContext.resume()` (Safari 27, Intel Mac). The context had been constructed fine at the requested 48kHz and the session sat on the dashboard, so the first user click hit `boot.ts`'s resume listener. OUR defect is the aftermath: the listener was `async` with no rejection handler (unhandled rejection, non-fatal branch, no dialog) AND `{once: true}`, so it had already been removed — audio was permanently dead for that session, silently, with no retry. Three sibling sites had the same hole: `EngineFacade.play()`, `StudioService.exportMixdown`, `ChopDialog.playSlice`. WHY WebKit refused is still open: the explicit `sampleRate: 48000` against a 44.1kHz output device is the hypothesis, unproven | Uncommitted: `AudioContexts.resume()` in studio-core catches, notifies once, and logs `state` / `sampleRate` / a probed hardware `sampleRate` so the next report settles the 48k hypothesis; `boot.ts` re-arms the click listener on failure; all four sites routed through it. A sampleRate fallback (rebuilding the context and engine graph) is NOT done, it waits on that evidence | live id 1108 |
| fixed | `Fullscreen request denied` | Shadertoy canvas `requestFullscreen()` promise rejected (Firefox) with no handler → unhandled rejection | `bd02d1664` (`Promises.tryCatch`) | live id 1028 |
| fixed | `Cannot use 'in' operator to search for '__id__' in null` | Worker `event.data` can be `null`; messenger `Channel` used `"__id__" in data` | `c1a1d6da4` (null-safe guard, drops `in`) | live id 1029 |
| fixed | `Could not remove …/1` (a `<SelectionBox>/1`) | `PointerHub.catchupAndSubscribe` onRemoved unguarded `added.removeByKey`; a selected box deleted while VertexSelection re-subscribes mid-transaction | `473b32ccb` (symmetric `removeByKeyIfExist` guard) | live id 1034; see [[project_boxgraph_incremental_edges]] neighbours |
| fixed | `duration(0) must be positive` | Zero-length audio sample (`numberOfFrames === 0`) → duration-0 audio region → `validateTrack` panic on next edit | `ebe74316b` (import guard) + `859221b85` (sample cleanup) + `ab3824d9f` (project-load migration `migrateZeroDurationRegions`) | live ids 998/1003/1035/1036 |
| fixed | `second part duration will be zero or negative(…)` (`RegionEditing.clip`) | Pre-`4c94199ec` separate branch: `#executeTasks` floored/ceiled a seconds-based mask and called `RegionEditing.clip` unconditionally, so a ceiled `end` a few µs past the region's drifted ppqn `complete` (-7.87e-6 on 1027) produced a negative second part. 1025/1026 (`duration(0) must be positive`) are the same build and day: the sibling sliver that stored as float32 0 and tripped `validateTrack` | `4c94199ec` (2026-07-07): exact `leftEmpty`/`rightEmpty` route a mask edge at or past the region edge to delete/trim, `clip` only runs on two positive parts. PR #364 (2026-08-28) wrongly re-attributes these ids to a postProcess bypass; there is none, `postProcess` is `#executeTasks`. It does close a residual right-edge hole (fractional mask end ceiling to within `boundaryTolerance` of a drifted `complete` still clips a sub-ulp sliver) via a tolerance-aware `classifySeparation`; `position` is Int32 so the left edge cannot drift | live ids 1025/1026/1027, all logged 2026-07-05 on build `1782807687669`, flipped fixed |
| fixed | `Pointer …(regions) …/1 requires an edge.` | Clipboard device paste bundles the unit's note tracks+regions; when NOT replacing, `excludeBox` dropped the `TrackBox` but not its regions → dangling mandatory `regions` pointer | `DevicesClipboardHandler` `excludeBox` drops whole timeline subtree (gated on `hasInstrument`) + regression test | live ids 1049–1051 |
| root-cause-known | `No CaptureMidi available` | A unit's capture became `CaptureAudio` (Tape) while note tracks/regions from a prior MIDI instrument remain; `NoteEditor.resolveCapture()` unwraps a `CaptureMidi` and panics. The creation path of that state is still unidentified (legacy / an untraced edit) | Load-migration `migrateCaptureTrackMismatch` deletes capture-mismatched content tracks (cascades regions) — cleanup only, not the creation source | live ids 1040–1048 |
| fixed | `The provided float value is non-finite.` (SVGLength) | `Range.scaleBy` (lib-std `range.ts`) divides by `range = max - min`; a zero-width range → `Infinity`/`NaN` min/max, which `set()` did not sanitize, so `TimelineRangeSlider.tsx:50` set a non-finite `SVGLength.width` and threw. Root: `set()` (the mutator all ops funnel through) did not uphold the minimum-width invariant the min/max setters do | `9dc7dc506` (`set()` upholds the minimum-width invariant + rejects non-finite input) | live id 1037 |
| fixed | `regions overlap: prev.complete(…) > next.position(…)` | Clip-mode only (`RegionOverlapResolver.apply` validates just the `clip` behaviour). A `RegionMoveModifier` (deltaPosition -1440, deltaIndex 0) approved a move that left a residual overlap. Sweep (`Region1054OverlapSweep.test.ts`, 7 layouts × regions × 12 deltas) shows single-region moves do NOT reproduce: for every region `validateTrack` checks (note / musical audio; seconds audio is `allowOverlap`-exempt) duration is position-independent so `complete === resolveComplete`, and the resolver's trim target matches the validation. ROOT CAUSE FOUND (2026-07-24): the panics fire on arbitrary edits (incl. a no-op, 1057), so the invalid state PRE-EXISTS the edit — the modifiers only detonate it. The proven creator: `AudioContentFactory.calculateDuration` returned a bpm-less sample's duration in SECONDS as if it were ppqn, so `RegionDragAndDrop.handleSample` built a near-zero clip mask (4 s = "4 ppqn" vs the real tempo-mapped 7680) — the drop left the regions underneath un-clipped, and validateTrack's seconds-exemption (`allowOverlap` checks prev only) hid the stack at drop time. 1080's log shows the exact sequence: file drop, then a loop-duration drag 53 s later → panic, `next.position(0)` = the drop position (the 0/480 signature across occurrences = bar-start drops). A second, still-open creator: `AudioContentModifier.switchTimeBaseToMusical` (StretchSelector) flips the exemption + rewrites duration with no resolver — converting legally-overlapping seconds regions (stacked drops, recorded takes) mints the invalid state silently; fix needs a product call (clip neighbors vs block) | Mask fix: `calculateDuration(sample, tempoMap, position)` converts the bpm-0 extent through the SAME tempo-map math as `TimeBaseConverter.toPPQN`, so mask == region extent by construction + `Region1080DropOverlap.test.ts` (fails pre-fix, green post-fix; 1054 sweep + resolver tests stay green). Diagnostic dump in `validateTrack` (`console.debug`s the full track layout: uuid, TYPE + TIMEBASE, position/duration/complete/loop/mute/mirrored + the offending pair into the shipped `LogBuffer`) confirms remaining creators in production; already-saved corrupted projects still panic until a load-time heal (existing `migrateAudioRegionOverlaps` only absorbs sub-ppqn truncation). Fix commit `7519aeffa`; all six ids flipped fixed=1 via `fix.php` on 2026-07-24 | live ids 1054, 1057, 1072, 1078, 1079, 1080 |
| fixed | `No worklet to subscribeDeviceMessage` | `DevicePanel` is bound to `projectProfileService`/`userEditingManager` (not the worklet), so it mounts a scriptable device editor (Apparat) whose ctor calls `subscribeDeviceMessage`. `#listenProject`'s `startAudioWorklet` failure path early-returned leaving the profile installed with `#worklet = None`, violating "profile installed ⇒ worklet present" | `f0a247dd0` (`StudioService.#listenProject` failure path routes the screen to `dashboard` so no worklet-dependent UI mounts) | live ids 1053, 1052 |
| fixed | `no device-host` (touch ghost) | `Surface.#listen()` missing-pointerup workaround fabricated a `pointerup` on the previously-pressed target for ALL pointer types. On touch a tap's up is lost, so the next event dispatched a ghost `pointerup` to the already-removed effect Delete menu item → `deleteEffectDevices` ran a SECOND time on the now-detached adapter → `deviceHost()` panics. Proven via captured logs (double `MenuItem.trigger: Delete '…'`) | `9daf4edd4` (`Surface.tsx` arms the recovery for `pointerType === "mouse"` only; touch/pen deliver reliable up+cancel) | live ids 1039/1038/1020 |
| fixed | `no device-host` (preset-decode) | `PresetService.applyPresetTo` deleted the target effect before `insertEffectChain`, in one transaction. A corrupt preset (`RangeError: Offset is outside the bounds of the DataView`) fails decode, so `insertEffectChain` (validates before mutating) returns a failure without inserting, but the `modify` callback returns normally so the delete commits anyway (modify only rolls back on a throw). The detached effect's follow-up Delete panics | `9910b2e05` (insert first, delete replaced effect only on success; guard `insertEffectChain` header read for truncated presets) | live id 1015 |
| fixed | `{ValueEventBoxAdapter position: … index: …} and {…}` | Collaborative (YSync) merge: two peers each add a value event at the same position, both picking index 0, so the merged doc holds two `ValueEventBox`es at `(position, 0)`. The `(position, index)` uniqueness invariant is not checked at `endTransaction`; it only trips the SortedSet comparator lazily in `asArray`, so the merge commits and crashes later on selection (`onSelected → onEventPropertyChanged → asArray`). `deterministicReconcile` only handled exclusive-target overflow | `b8e3713f7` (add a value-event rule to `deterministicReconcile`: per position first@0/last@1, delete surplus, uuid-tiebroken; mirrors `migrateValueEventCollection` so reconciled + freshly-loaded graphs converge) + `Reconcile.test.ts` | live id 1047 |
| fixed | `Cannot access file.` | RECURRED 2026-08-08 (1094), so the parked row is reopened, and the deployed sourcemap now resolves the frame the earlier analysis could not: `AudioClipPainter.ts:10` (`const {file, ...} = adapter` → `file.peaks`) reached from `CanvasPainter.render` on an ANIMATION FRAME (lib-dom `frames.js:43`), i.e. NOT inside a transaction at all. 1094's log ends `MenuItem.trigger: Convert to Clip` … `MenuItem.trigger: Delete` → crash: `Clip.tsx:75` subscribes `adapter.subscribeChange` and calls `painter.requestUpdate()`, deletion dispatches that change, and the queued repaint runs on the next frame against an adapter whose `#fileAdapter` is already `Option.None`. Sibling renderers already read the safe `optFile`; this painter is the one that hard-unwraps | Fixed (uncommitted) — and the first attempt (an `optFile` guard in the painter) was REVERTED as a band-aid. A graph probe settled it: deleting the region leaves the converted clip intact (`optFile` still resolves), deleting the CLIP is what empties it, and the owning lane's `clips.collection` emits `onRemove` INSIDE the deleting transaction — so for its own track the cell clears and the painter dies before any frame, no guard needed. The hole is `ClipLane.populatePlaceholder` borrowing clips from ANOTHER track through `strategy.translateTrackIndex` while a move runs, when the lane subscribes to its own collection only: that cell never hears the removal and keeps the dead adapter until the DEFERRED (`deferNextFrame`) rebuild a frame later. Fix: each cell owns a `box.subscribeDeletion` on the adapter it displays and nulls itself, so the `ClipPlaceholder` terminates the `Clip` (and its painter) in the deleting transaction whichever track owns the clip. `ClipCellLifetime.test.ts` pins it. NOT PROVEN: that this borrow path is the one 1094 took — its log shows no clip drag at the end, so if it recurs the remaining path is elsewhere | live ids 1021, 1094, 1097. RECURRED AGAIN 2026-08-18 (1097) on build `8684107e` (main, 2026-08-17), whose deployed `sourcesContent` contains the ClipLane cell guard — so that fix is shipped and does NOT close this. The 1097 frames sourcemap to the SAME point (`AudioClipPainter.ts:10` ← `CanvasPainter.render` ← `frames.js:43`), but its log holds no clip action at all: New Project, Tape unit, Mixdown, Autotune, an audio recording take, then `Delete` on the take REGION → `TimelineFocus: region deleted` → crash on the next frame. So the surviving trigger is an ALIVE clip whose `optFile` is empty (the `file` POINTER cleared, not the clip box deleted) — `AudioClipBoxAdapter`'s `file.catchupAndSubscribe` sets `#fileAdapter = None` and the sibling `Propagation.Children` subscription dispatches a change, which `Clip.tsx:76` turns into `painter.requestUpdate()`, so the very change that empties the file schedules the paint that unwraps it. A cell guard keyed on clip DELETION cannot see this. Where the clip and the empty pointer came from is not in the log (clip creation by drop is unlogged); so a probe now sits in `AudioClipPainter` ahead of the unwrap (uncommitted): on an empty `optFile` it warns the clip uuid, label, `box.isAttached()`, the `file` pointer ADDRESS and whether that address still resolves to a vertex, and the track uuid. Diagnostics only, the panic still follows, so the next report carries the answer in its LogBuffer. The address-vs-resolves pair is the decisive bit: an address that no longer resolves means the AudioFileBox was deleted under a live clip, an empty address means the pointer itself was cleared |
| root-cause-known | `Storage not available` | Two distinct causes, same message (thrown by `OpfsWorker.getOpfsRoot`, OpfsWorker.ts). (1) 1055/1024: `ProjectProfileService.save`/`saveAs` were the only storage flows not wrapped in `tryCatch`, so a mid-session OPFS rejection escaped as an uncaught crash. (2) 1056: the availability gate probes the WRONG runtime — `features.ts:10` + `boot.ts:148` check `navigator.storage.getDirectory()` on the MAIN thread, but every real op runs in the WORKER. Firefox can resolve `getDirectory()` on the window yet reject it in a DedicatedWorker (privacy/persistence config), so boot green-lit a session where the worker could never reach OPFS and the first uncaught worker op crashed (my earlier "evicted after boot" theory was wrong) | 1055/1024: `92936c29a` (wrap both writes; retryable notice, work preserved). 1056: worker-side `OpfsProtocol.isAvailable()` (pings `getOpfsRoot` in the worker) + `boot.ts` gate now awaits `Workers.Opfs.isAvailable()` instead of the main-thread call, so a worker-OPFS-less session shows the "Storage Unavailable" dialog and never starts + `OpfsWorker.test.ts` regression (uncommitted) | RECURRED on post-fix builds via the worker READ path (1067/1075): the boot gate + save-wrapping do not cover raw `read`/`write` calls — see the OPFS caller audit below. (3) 1067/1075/1109 (2026-08-26, Firefox 154 macOS, build `6cb94651` WITH the worker gate): all three open the SAME demo bundle `7e0dc330…` from the dashboard with no profile and die 30 ms..5 s after `VertexSelection.switch`; the full 1075 stack ends in `Object.write` → `#acquireLock` → `#resolveFile` → `getOpfsRoot`, i.e. the demo flow's RAW `await profile.saveAs(profile.meta)` (`DemoProjectsList.tsx:68`) bypasses the wrapped `ProjectProfileService.saveAs` and escapes as an uncaught rejection. Sibling raw callers: `NextcloudDialogs.tsx:143/149` (`saveAs`), `PublishMusic.ts:48` (floating `profile.save()`). Open question: in 1109 the worker probe returned true while `read`/`list`/`write` in the same 20 ms window all rejected `getDirectory()` (PresetStorage index failed at boot), so Firefox can pass the probe and still have no worker OPFS. Fix (uncommitted, 2026-08-26): all three raw callers wrapped in `Promises.tryCatch` + warning toast, project still opens in memory (`DemoProjectsList.tsx`, `NextcloudDialogs.tsx` `persist()`, `PublishMusic.ts`); `OpfsWorker.getOpfsRoot` now throws `Storage not available (<name>: <message>)` so the next report shows WHY `getDirectory()` rejected (worker console is not captured, only the error crosses the Communicator) + `OpfsWorker.test.ts` case. live ids 1055, 1024, 1056, 1067, 1075, 1109, all flipped to fixed in the DB on 2026-08-26 |
| root-cause-known | `A requested file or directory could not be found` (NotFoundError DOMException) | A `read` of a missing OPFS file. `delete`/`list`/`exists` `tryCatch` internally (no-op / empty / bool), so only `read` throws NotFound; the raw read callers (enumeration loops + load-by-uuid) propagate it. See the OPFS caller audit below | Fix order: group 1 (enumeration skips) first, then load-by-uuid caller-by-caller | live ids 1058, 1059 |
| fixed | `A requested file or directory could not be found` (P2P host, `[P2P:Server] reading sample from OPFS...` precedes it) | Live-room HOST crash. `AssetServer.#onAssetRequest` answers the peer's inventory with `hasSample` = `SampleStorage.exists(uuid)`, which only checks the sample FOLDER (SampleStorage.ts:41-43). The transfer then calls `readSample`, which reads `audio.wav` + `meta.json` (StudioLiveRoomConnect.ts:50-57). A folder without both files (partial `save`, the three writes run in parallel and a request landing mid-save sees the folder first, or a failed/aborted save) passes the inventory and throws NotFound on read. That throw is UNHANDLED: `channel.onmessage` calls `this.#onChannelMessage(...)` with no `.catch` (AssetServer.ts:100-103), unlike the three signaling handlers (AssetServer.ts:38-44). The requesting peer already copes (stall timer + retries, PeerAssetProvider.ts:146). | Fix: (1) `hasSample` checks `audio.wav` AND `meta.json` exist, not the folder; (2) `#onChannelMessage` gets the same `.catch(console.warn)` as its siblings so a read failure never escapes the host; (3) the transfer answers with a Cancel so the peer stops retrying instead of stalling. | live id 1110. `a0d83814f`: `hasSample` checks both files, `#onChannelMessage` caught, host sends `Cancel`, provider handles `Cancel` via `#retryTransfer`; regression test `AssetServer.test.ts` "answers with Cancel" (fails without the fix, 5 s unhandled rejection) |
| fixed | `undefined is not an object (evaluating 'p.key.toLowerCase')` | `FloatingTextInput.tsx:36` `inputField.onkeydown` dereferences `event.key`. A real `KeyboardEvent` always carries `key` (at worst `""`/`"Unidentified"`), so `undefined` means the `keydown` was dispatched as a plain `Event` by something outside the page. UA is DuckDuckGo browser for macOS (`Ddg/26.6`, WebKit), whose autofill/content scripts synthesize input events on focused text fields. Occ=1. Same raw dereference at `monaco/factory.ts:44,50`. | Fix: guard with `isDefined(event.key)` at all three sites (foreign input, not app state). | live id 1111. `a0d83814f`: `isDefined(event.key)` at all three sites |
| root-cause-known | `No profile available` | Stack-proven: a worklet-restart chain outlives the project. `Project.startAudioWorklet`'s error handler (Project.ts:272-280) awaits `restart.unload` (the "Restart Engine" dialog) then unconditionally restarts and calls `restart.load`, which in `StudioService.#listenProject` (StudioService.ts:518-521) does `switchScreen(capturedScreen)` with NO profile check. If the profile was closed meanwhile (Android back → route "/" → `StudioNavigation.closeProject` sets `Option.None` silently when there are no unsaved changes), the restarted chain mounts a project screen whose components read `service.project` → `get profile` (StudioService.ts:394) unwrap panics. 1083's log: Fire tablet, engine died 3× ("Retro Game", warn `{"isTrusted":true}` per death), crash fires from `load → switchScreen → buildScreen`. The exact profile-clearing trigger on the device (back-nav vs other) is inferred, the missing guard is proven by the stack | Fixed (uncommitted): (1) `Project.#terminated` flag set in `terminate()`; the restart handler bails after `unload` when terminated (no engine resurrection for a closed project). (2) Both `RestartWorklet.load` closures (`restartEngine` + `#listenProject`) verify the profile/project is still current before `setWorklet`/`switchScreen`. (3) Central belt: `StudioNavigation.switchScreen` re-derives any non-dashboard key without a profile through `closeProject()` (dashboard + "/"), closing every stale-caller sibling. Regression: `RestartChainTerminated.test.ts` (fails without guard 1, green with it). Flip id 1083 in the DB once deployed | live id 1083 |
| fixed | `trackBoxAdapter` | `Box.isAttached()` is UUID-based (`graph.findBox(uuid).nonEmpty()`, box.ts:122), so it cannot detect an ORPHANED INSTANCE. 1095's log shows a `RegionLoopDurationModifier` drag in flight when the user pressed Delete and then Undo (`start(...)` … `TimelineFocus: region deleted` … `undo` … `approve(...)` → crash). Undo re-creates the region under the SAME uuid, so the modifier's captured, orphaned adapter passes `approve`'s `filter(({box}) => box.isAttached())` guard (RegionLoopDurationModifier.ts:119) while its own `regions` pointer field stays disconnected — `trackBoxAdapter` resolves live from that field, so it is `None` and `RegionClipResolver.fromSelection` (RegionClipResolver.ts:55) hard-unwraps it. Sourcemapped: fromSelection ← `RegionOverlapResolver.apply` ← `RegionLoopDurationModifier.approve:126` ← `TracksManager.ts:177` ← lib-dom `dragging.js:131`. Same `approve` also builds `modifiedTracks` with `unwrapOrNull()`, so the two lists disagree and `asDefined(clipResolvers.get(index))` would fail one line later anyway | Fixed (uncommitted): `RegionModifierSelection.isIntact` / `.tracksOf`. All FOUR modifiers that shared the `isAttached()` guard (LoopDuration, Duration, ContentStart, Start) now ABANDON the drag (`return this.cancel()`) when any captured region has lost its track, rather than applying to the survivors — a partial edit is not what the user dragged for. Filtering was the first attempt and was replaced; `RegionMoveModifier` already filtered this way, which is why its approve survived in 1095's log while the LoopDuration one crashed. `RegionModifierSelection.test.ts` rebuilds the orphan (delete + re-create under the same uuid) and asserts the trap (`isAttached()` true, `trackBoxAdapter` empty), the crash (`fromSelection` throws on the orphan) and the guard. NOT changed: `Box.isAttached()`'s uuid semantics, and the other staleness callers that share the hazard (SampleSelector, Slot.tsx, RecordAutomation) | live id 1095 |
| fixed | `Failed to fetch` (sample index) | Not a status code (a status takes the `panic("${response.status} …")` branch), so the transport itself failed: no response at all for `assets.opendaw.studio/samples/index.json` on an offline blip / reset connection / blocked request. The session was online (`OpenPresetAPI.list loaded 47 cloud preset(s)` at boot), the index fetch 2.6 s later died alone. Three unguarded layers stacked: (1) the memoized index fetch in `OpenSampleAPI.ts:38` was the ONLY network call in that file without a retry, every sibling (`get`, `load`, and the soundfont equivalents) already used `Promises.retry(() => network.limitFetch(...))`; (2) `AssetService.replaceMissingFiles` awaited `collectAllFiles()` raw, and `ProjectProfileService.load:155` awaits `replaceMissingFiles` OUTSIDE its `Promises.tryCatch` (that only wraps the storage read), so the rejection escaped the whole load; (3) `Resources.tsx:51` awaited `load` with nothing around it, so `handler.close()` never ran and the modal `Dialogs.processMonolog("Loading...")` stayed on screen forever. ErrorHandler took the `PromiseRejectionEvent` branch (no crash dialog, session alive), so the user-visible symptom is a permanently hung "Loading..." overlay, not a crash | Fix: (1) both index fetches (`OpenSampleAPI`, `OpenSoundfontAPI`) go through `Promises.retry(() => network.limitFetch(...), new IntervalRetryOption(3, TimeSpan.seconds(1)))` — only the TRANSPORT retries, a status code is a real answer and is not re-asked; a fresh `IntervalRetryOption` per call because the module-level `DefaultRetryOption` is a shared singleton whose `#count` never resets. (2) `replaceMissingFiles` `tryCatch`es `collectAllFiles` and SKIPS the scan on failure — it must NOT degrade to an empty list, that would flag every stock asset as missing and open a browse dialog per box. (3) Both `Resources.tsx` select handlers (project load + template open) close the monolog unconditionally and notify on rejection. `AssetServiceCatalogUnreachable.test.ts` (2 of 3 fail pre-fix, green post-fix) | live id 1096; flip in the DB once deployed |
| environmental | `null is not an object (evaluating 'e.webkitEnterFullscreen')` | Top frame is `user-script:7:1` — a Safari userscript/extension monkey-patched `requestFullscreen` and its shim dereferences a null video element (`webkitEnterFullscreen` exists only on HTMLVideoElement, never canvas). Our `ShadertoyPreview.tsx:65-69` click handler is correct; the shim throws SYNCHRONOUSLY while the argument of `Promises.tryCatch(...)` is evaluated, so the throw escapes the wrapper inside the async listener → unhandled rejection reported fatal | Optional hardening: evaluate the fullscreen call inside the guard, e.g. `Promises.tryCatch((async () => document.fullscreenElement ? document.exitFullscreen() : canvas.requestFullscreen())())`, so foreign sync throws are contained | live id 1082 |
| fixed | `cancel` | `FloatingTextInput` rejects its resolvers with the raw STRING `"cancel"` (FloatingTextInput.tsx:33, on `input.onblur`), not with `Errors.AbortError`. Every older call site passes `EmptyExec` as the rejection handler and swallows it, so the bug stayed invisible; the two modulation editors classify instead — `if (!Errors.isAbort(error)) {return panic(...)}` — and `Errors.isAbort("cancel")` is false for a plain string, so a cancelled edit is reported as a crash. Sourcemapped on build `8881081e` (dev): `StepsDisplay.tsx:114` (`panic(String(error))`), the sibling is `ModulatorEditor.tsx:31`. 1098's log matches exactly: `MenuItem.trigger: Steps` (ModulationPanel adds the Steps modulator), 2.7 s later a warn about a node removed in a blur handler, then the panic — double-click a step, then click away | `e7f46b731` (reject with `Errors.AbortError` in `FloatingTextInput`, so `isAbort` holds at every call site; the `EmptyExec` sites are unaffected, the two classifying sites return silently as designed, and `ErrorHandler.#tryIgnore` drops the rejection before the reporting branch) | live id 1098, flipped fixed=1 via `fix.php` on 2026-08-19 |
| unknown | `messageerror` | `lib-runtime` messenger.js:14 rethrows `port.onmessageerror` — a structured-clone deserialization failure on some port (worker/worklet). Occ=1, Safari TP (Version/26.5), log shows a normal project load; the event carries no payload identifying the port or message. Not diagnosable from one sample; if it recurs, the messenger should log WHICH port/channel before throwing | — (revisit on recurrence) | live id 1081 |
| environmental | `NS_ERROR_UNEXPECTED … get width` | Firefox 152/Linux threw its internal `NS_ERROR_UNEXPECTED` reading `window.innerWidth` (`Surface.tsx:186`) — a Gecko window-teardown/session-restore quirk, not app state. Occ=1 | — | live id 1073 |
| fixed | `Failed to load because no supported source was found.` | A media element failing to load its source (browser lacking the codec, "no supported source") dispatches a plain `"error"` Event (target = the element), not an `ErrorEvent`/`PromiseRejectionEvent`. `ErrorHandler.#tryIgnore` only filtered ErrorEvent/CSP/Monaco, so it fell through `processError`'s fatal path and was reported as a crash. The underlying media failure is environmental, but reporting it as fatal was a classification bug | `723431ddb` (ignore resource-load errors: a plain `"error"` Event whose `target instanceof HTMLElement`; real JS errors arrive as `ErrorEvent` and are unaffected) | live id 1022 |
| environmental | `Permission denied to access property "then"` | Gecko's cross-compartment security wrapper refusing `Get(obj, "then")` while the promise machinery probes a thenable, i.e. the awaited object lives behind a security wrapper. gatedDAW cannot trip this on its own: the app has zero iframes, and the only cross-origin windows it opens (OAuth popup in `CloudAuthManager`, Tone3000 popup) are never awaited and appear in neither log (both sessions sit on the dashboard, AudioContext still suspended). The reporter runs a privacy-hardened Firefox: the UA `(Windows NT 10.0; rv:102.0)` with no platform token is the `privacy.resistFingerprinting` spoof, while the same session logs `Promise.withResolvers available`, which real FF102 does not have. So an extension content script or hardening shim (a patched `fetch` is the likeliest) handed page code a promise from its own compartment. The THROW is environmental, reporting it as FATAL was ours: the error carries an EMPTY stack, and both `#looksLikeExtension` and `#extractForeignOrigin` return early on an empty stack (ErrorHandler.ts:56), so it was classified as our own code and took `processError`'s fatal path (`AnimationFrame.terminate()` + crash dialog). The captured log confirms the branches taken (`looksLikeExtension:false, foreignOrigin:null, scriptsCount:1`), and the user reloaded and crashed again 2.7 min later (1105 died 21 s after boot, 1106 2.7 s after boot) | Fixed (`57c6e9fdf`, deployed in build `d94c6784` of 2026-08-21 12:21): `#tryIgnore` now matches `BrowserInternalPatterns` on the `ErrorEvent` branch as well (it was rejection-only, so every browser-internal error arriving as an `ErrorEvent` shared this hole, incl. the `NS_ERROR_UNEXPECTED` family), checking both `event.message` and `event.error.message`, and the pattern list gains `Permission denied to access property` / `Permission denied to access object`. Both branches route through `#notifyBrowserInternal`: `preventDefault()` + `console.warn` + a `Dialogs.info` "Warning" (browser/extension blocked an operation), shown ONCE per session (a shimmed API can throw on every call) and only when `Surface.isAvailable()`. Session stays alive, no report sent. The rejection branch now keys on `reasonMessage` so a string reason matches too. No regression test: `ErrorHandler` imports `Surface.tsx`/`dialogs.tsx` at module level, so it is not loadable under the node test env | live ids 1105, 1106; flip in the DB once deployed |

## OPFS storage errors — `Storage not available` + `NotFoundError` caller audit (2026-07-24)

Both live errors come from the same `OpfsWorker` layer and share one analysis.

**Already safe by construction.** `delete`, `list`, `exists`, `isAvailable` each `tryCatch` internally: `delete`
no-ops on any failure (incl. NotFound and Storage-not-available), `list` returns `[]` for a missing/unavailable
folder, `exists`/`isAvailable` return a bool. So every delete/list/exists caller (~55 of the 98 call sites) is
already covered — none can crash.

**The only throwing surface.**
- `read` → `NotFoundError` (missing file) or `Storage not available` (getOpfsRoot fails)
- `write` → `Storage not available` only (it creates parents via `{create:true}`, so no NotFound)

So `NotFoundError` is always a `read` of a missing file, and `Storage not available` rides the same read/write
calls. Hardening the reads covers both.

**read callers — guarded (already graceful):** `Recovery.ts:20-26` (inside `tryCatch(Promise.all)` → `None`),
`Storage.ts:21`, `ProjectStorage.ts:40/51/65/67/95`, `TemplateStorage.ts:41/84/86/114`,
`PresetStorage.ts:67/125/139/170`.

**read callers — raw (propagate), all recoverable:**
1. Enumeration reads (one bad entry breaks the whole listing) — the clear bug; fix = skip the unreadable entry
   like `ProjectStorage.listUsedAssets` already does: `ProjectStorage.ts:29`, `TemplateStorage.ts:30`,
   `Storage.ts:36`.
2. Load-by-uuid (missing → surface "not found", not crash): `ProjectStorage.ts:43/47`, `TemplateStorage.ts:44/48`,
   `SampleStorage.ts:53/62/63/64`, `SoundfontStorage.ts:35/37`, `PresetStorage.ts:98`, `NamTone3000.ts:57/71`.
3. Export/bundle (missing referenced file → export fails gracefully): `ProjectBundle.ts:109/110/111/133/134`.
4. Cloud: `SharedFolderSync.ts:383`, `StudioLiveRoomConnect.ts:53/54`.

**Verdict.** None of the raw reads is genuinely unrecoverable — every one has a sane fallback (absent / skip /
failed-load notice). Root-fix order: group 1 (enumeration skips, self-contained and clearly correct), then
groups 2-4 caller-by-caller (each needs a fallback decision: `Option.None` vs a user-facing notice). `write`
throws only Storage-not-available; the save path is wrapped (`92936c29a`), other writes remain raw but only fail
when storage is genuinely gone.

## How to extend

When the task diagnoses a NEW error group:
1. Add a row with `investigating` (or `unknown` if the stack is opaque), the message signature, and what the
   stack/logs reveal.
2. If the root cause is understood, set `root-cause-known`, describe it, and start the fix.
3. On landing a fix, set `fixed` and record the commit(s). Then mark it fixed in the DB (see below).

## Marking fixed in the DB

`unfixed.php` returns rows where `errors.fixed = 0`. Flip a shipped group's rows with the `fix.php` write
endpoint (POST only, ids from the group's `ids` field):

```
curl -X POST "https://logs.opendaw.studio/fix.php" -H "Content-Type: application/json" -d '{"ids": [1053, 1052]}'
```

Responds `{"updated": <rows>, "ids": [...]}`; verify with `unfixed.php` afterwards.
